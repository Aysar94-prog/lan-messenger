# LAN Messenger handoff — Android / Windows 0.8.7

Current local layout: `D:\LAN-Messenger\source` is the canonical Git source repository; `D:\LAN-Messenger\outputs` contains releases and historical build/test material. Android signing key is private at `source\.private\development.keystore` and is excluded from Git and source archives. Historical paths below describe where older work was originally done.

## 0.8.7 - destination downloads and plaintext Fast payload

Confirmed scope: Android and Windows. Download asks for a destination before network transfer. Files stream directly to the selected location, with no duplicate private attachment. Clicking a completed file opens the OS viewer. Clearing chat never deletes the user-selected file. Normal photos still download automatically inline.

FETCHDIRECT uses continuous TLS for normal files and a separate plaintext TCP data socket for Fast files, authorized via a one-use random token on the pinned TLS control connection. File bytes and final destination are unencrypted for Fast; authentication, message history and destination metadata remain protected. Both peers need 0.8.7 for manual downloads. Old Fast requests receive FASTONLY; normal automatic photos retain legacy FETCH / FETCHSTREAM support. Fast draft identifies unencrypted transfer.

Android uses ACTION_CREATE_DOCUMENT, persisted SAF grants and a seekable local descriptor. Windows uses SaveFileDialog and shell file associations. Partial downloads resume at 256 KiB boundaries from the chosen file; final SHA-256 verifies content. Hash mismatch removes the destination reference for retry, preserving the damaged user file. Previously cached files are copied out and private copies removed after success.

Tests: direct_downloads.py validates bidirectional Java/C# normal and Fast transfers, single-copy storage, plaintext socket reads, wrong token and token reuse rejection, Android-to-Android disconnect with monotonic progress, restart and clear preservation. transfers.py covers direct pause/restart, source mutation, chat responsiveness and 128 MiB transfers under 64 MiB heap. Existing secure integration, image, quota, encrypted resume and UI tests remain. Full suite result and hashes: outputs/Release-Notes-0.8.7.md. Physical Android picker/viewer and real Wi-Fi speed require device testing.

Android versionCode 21 / 0.8.7 signed with original key; Windows 0.8.7 requires .NET 9 Desktop Runtime as before. Keep changes local; no push requested.

## 0.8.6 — Android transfer performance and persistent resume, 2026-09-24

User reported ordinary Android-to-Android downloads still slow and restarting after 0.8.5. User explicitly approved changing the Android file protocol to one continuous stream with 256 KiB checkpoints while retaining network and local-file encryption. Both phones need 0.8.6 for continuous resume; legacy peers remain on FETCH.

Found two hot-loop problems: CipherInputStream's small reads caused very many network writes, and each iteration rehashed/formatted the same TLS certificate and scheduled a watchdog. Cancelled watchdogs stayed queued for their 45-second timeout. Reads now fill a 256 KiB network buffer, the per-session fingerprint is cached while its pin is still rechecked, and cancelled scheduled tasks are removed immediately. A direct 205 MiB loopback comparison measured 12.38 s with 0.8.5, 2.23 s with the hot-loop fix, and 1.93 s with continuous transfer. These are host Java engine tests, not measured phone/Wi-Fi speeds.

New ResumableTransfer.java negotiates FILECAPS/STREAM1 and serves FETCHSTREAM over the existing pinned mutual-TLS connection with unchanged direct/group authorization, file slots and daily upload accounting. ResumeStore.java keeps .sec.resume encrypted with the existing OS-wrapped AES-CBC key and LMATCS1 layout. Complete 256 KiB blocks continue the CBC chain; only the final block receives PKCS padding. Interrupted blocks are truncated on reopen. Final SHA-256 is calculated while receiving, or rebuilt once from the saved prefix after restart. Completion rechecks cancellation/retention/trust and renames to .sec; saved legacy 100 MiB segments can be imported. Pausing closes the active socket. Local storage errors and full-file corruption stop rather than silently restarting; retry/wait/error notes appear below the file.

The first full suite exposed a mixed-group source-selection hang: a modern peer with only an offer was chosen over a legacy peer holding the file. Fixed by preferring the original sender's available protocol and falling back to legacy relays when modern sources have no data and no new progress has been saved. mixed_transfer_sources.py verifies both a legacy original and a legacy relay while modern offer-only peers are online. Integration harness commands now have bounded waits and per-process stderr logs rather than hanging without diagnostics.

Tests added: TransferWatchdogTest (10,000 writes leave no cancelled guards), ResumeStoreTest (padding boundaries, torn blocks, old decoder compatibility and legacy checkpoint import), android_resume.py (socket disconnect, monotonic progress, pause/restart, abrupt process termination, wrong recipient, invalid offsets, changed source and clear during transfer), and mixed_transfer_sources.py. android_ordinary_transfer.py verifies 205 MiB of distinct blocks through the continuous path. Focused tests passed; full-suite final status is recorded with the release notes. Android versionCode 20/versionName and About 0.8.6; original signing key retained. Physical Android and real Wi-Fi validation still require the user's phones. Keep changes committed locally; no new push was requested for this fix.

Final verification: full tests/run.ps1 passed with exit 0, including secure integration, features/groups/relay, images, upload policy, legacy transfers, 205 MiB continuous transfer, forced disconnect/pause/process-crash resume, mixed protocol source selection and native Windows UI. Final loopback transfer: 205 MiB plus verification in 1.95 s (105.0 MiB/s). Build/test results are under `D:\LAN-Messenger\outputs\.build\release-tests-086`. APK rebuilt after the mixed-group fix; v2/v3 signature verified and signer SHA-256 matches 0.8.5. Release notes and APK SHA-256 are in outputs.

## 0.8.4 — Android conversation latency, 2026-09-23

Android now initially renders the latest 10 messages and reveals 20 older messages per upward scroll. A 16 MiB in-memory bitmap cache avoids repeated decrypt/decode for visible images, and non-image attachments are no longer probed as thumbnails. Text sends update the UI after encrypted local persistence, before attempting network flush. Android manifest versionCode 18/versionName 0.8.4, About version 0.8.4, APK filename LanMessenger-0.8.4.apk; original signing key retained. Windows stays 0.8.3. Java/Android compilation, daily policy test, APK build/signature verification passed. Full cross-platform suite could not start its loopback peer on 127.0.0.2 in this Windows environment, and no physical Android device appeared in adb, so actual latency remains to be measured.

Updated 2026-09-23. Continue this existing project; do not rebuild from scratch.

## 0.8.3 — automatic incoming images, 2026-09-23

User explicitly changed the manual-download requirement: images should download automatically and display normally in chat. Other attachments remain manual. Applied to BOTH Windows and Android; no mode switch or additional approval is required.

Both engines recognize JPG/JPEG, PNG, GIF, BMP, WebP, TIFF, HEIC/HEIF and AVIF extensions case-insensitively. Automatic background tasks call the existing authenticated FETCH downloader, capped at two automatic image tasks per device. Saved incoming image offers are considered after startup/reconnection too. Only verified/reachable candidate sources are scheduled; group members can supply complete images. No payload was added to OFFER/META and no protocol downgrade. Existing integrity checks, resumable segments and Android daily upload accounting continue unchanged. Sender drafts still require explicit Send.

Each image gets one automatic attempt per session; Pause or final integrity failure will not be immediately overridden by the periodic scheduler. User can manually retry. Session restart may automatically try pending images again. Offline sources keep an already-started downloader retrying as before. Classification is by filename extension; raster decoding occurs only after full integrity verification, using existing decoder/pixel/20 MiB guards. Unsupported formats or oversized previews can still require Save/opening externally. This change does not add new image codecs.

Windows version 0.8.3; Android versionName/APP_VERSION 0.8.3, versionCode 17, original signing key. Each RECEIVER needs this update for automatic photos; existing 0.8 senders remain wire-compatible. Windows remains uncapped; Android daily tiers remain active.

Tests updated for automatic images versus explicit Download for binary files. New images.py checks group auto-download on Java/C# and that videos/double-extension .png.exe offers remain manual. Native Windows test separately verifies automatic image completion and an inline PictureBox without clicking Download. Work/results: new-chat-2/work/v083. APK built/signature verified; standalone Windows build has 0 warnings/errors. Verification complete: full tests/run.ps1 passed (secure integration, features, daily policy, real capped uploads, resumable large transfers, native Windows UI). Additional images.py passed separately: group images fetched without DOWNLOAD commands on Java and C#, videos and .png.exe stayed manual. Native UI confirms automatic-photo.png becomes an inline PictureBox without a click, and chat-switch/card-retention regressions pass. Visually inspected windows-switch.png under v083/ui-results/ui-194252c2e0a3466c8c6f86df48117213 (synthetic 1px fixture, not a real photo). APK signature v2/v3 verified. Physical Android UI/codec behavior remains untested. Release outputs: LanMessenger-0.8.3.apk, LanMessenger-Windows-0.8.3.zip, source archive, notes and checksums in new-chat-2/outputs.

## Android 0.8.2 — approved daily upload tiers, 2026-09-23

User explicitly approved Android-only implementation to reduce remaining session usage. Implement only the agreed per-device daily upload policy: <2 GiB unlimited, >=2 GiB 30 MiB/s, >=5 GiB 20 MiB/s, >=10 GiB 10 MiB/s, aggregate across all served file streams. No Windows code change and no additional proposed anti-flood rules.

New android/src/net/lanmsg/chat/DailyUploadPolicy.java is Android-independent Java for real-engine testing. It wraps outgoing FETCH payload writes in PeerEngine. Both normal snapshots and original Fast file sources (plus group relays) therefore share accounting/throttling. Text, offers, preparation, incoming files and avatars bypass it. A synchronized policy lock serializes bounded 256 KiB write slices and pacing, independent of the engine state lock. Tier boundaries split slices exactly; monotonic time paces capped output and does not accumulate a large idle burst.

daily-upload.sec is protected by the existing OS storage protector; data format LMUPLOAD1, local date, byte count. Count successful output writes, not offers or attempted writes that throw. Flush every 4 MiB or one second of active output and in transfer finally/service close, with synced temporary file and replacement. Abrupt crashes may lose approximately 4 MiB; graceful restart retains exact completed count. Actual partial socket bytes on a failed write cannot be observed by this OutputStream API. Invalid protected accounting fails startup rather than silently granting a fresh quota. Day advances at local midnight, including while running; backwards clock changes do not reset it. Clearing conversations leaves accounting alone. This is not centralized or tamper-proof against app data deletion, altered apps, rooted devices or manual forward clock changes.

Profile dialog displays uploaded GiB and cap in MiB/s. APK versionName/APP_VERSION 0.8.2, versionCode 16, existing signing key. Windows remains 0.8.1 and uncapped.

Tests: new DailyUploadPolicyTest.java exercises exact 2/5/10 GiB boundaries with an injected clock, concurrent aggregate cap, persistence, midnight, rollback, failed writes and corrupt storage. New upload_policy.py exercises actual Java→C# normal/fast transfers, no accounting before Download, saved usage across engine restart, two simultaneous 32 MiB downloads sharing the 10 MiB/s cap, and live text delivery. Harness-only SEEDUSAGE command reaches higher tiers without uploading 10 GiB; production has no such API. tests/run.ps1 includes the new Java source and policy tests.

Scratch logs/build: new-chat-2/work/android082. Device UI/Keystore performance and real Wi-Fi still require physical Android testing. Release is an APK update and source archive in new-chat-2/outputs. Final verification passed: DailyUploadPolicyTest (all thresholds, concurrency, date/persistence/error cases); upload_policy.py (64 MiB across two concurrent uploads in 8.432 s under the shared 10 MiB/s cap, normal/fast accounting and restart); features.py (groups/files/clear/receipts/relay/expiry/avatars); transfers.py (128 MiB, resume, integrity, authorization and chat during transfer). Android APK built and existing-key v2/v3 signature verification passed. Physical Android Profile/Keystore and Wi-Fi remain untested. No Windows source changes.

## Windows 0.8.1 — chat switching repair, 2026-09-23

User reported white/overlapping areas when switching chats. Windows-only UI patch:
- Nested sender/action FlowLayoutPanels now use GrowAndShrink instead of retaining their default blank height.
- Render is guarded against layout/Resize reentrancy; a pending update is deferred until the current render completes.
- Switching/rebuilding resets the old scroll offset before removing controls. Scroll and switched feed invalidate child windows to repaint exposed areas.
- Existing card retention for progress/new messages remains intact. No protocol, storage or Android change.
- Windows version 0.8.1; Android stays 0.8.0 and is compatible.
- Native UI suite passed (exit 0), including six repeated switches between a group and direct conversation while scrolled, compact sender/action row heights, non-overlapping cards, progress retention, notifications and explicit Download. Build 0 warnings/errors. Screenshot windows-switch.png visually inspected under new-chat-2/work/v081-ui/results/ui-4f705664ae1c4114a9d30e184e811abb.
- Deliver updated Windows ZIP in new-chat-2/outputs. Exit the old app through tray Exit, extract and run the updated executable. Local data stays in the existing app-data directory. User's exact physical desktop/DPI remains an acceptance check.

## Previous continuation — 0.8.0, 2026-09-23

Scope accepted by the user: fix transfer-induced chat latency and Windows blinking, blue/gray presence, dedicated fast-large-file action separate from the normal 1 GiB action, and recipient-triggered Download for **both** modes. Continue locally; do not add a remote or push. Original source tree retained; untracked windows-dist-v07.7z was left untouched.

### Implementation

- Both engines now send file OFFER metadata without bodies; group SYNCREQ2/META also carries metadata only. Legacy pushed file MSG is rejected before body consumption. An ACK/Delivered status confirms the offer, not downloaded bytes. Both ends need 0.8.0 for attachments/group synchronization.
- New FETCH/DATA/PART flow uses independent mutual-TLS pinned connections, authorized against retained direct recipients or group members. Two concurrent serving streams reserve capacity for chat. Bad ranges/unauthorized requesters are rejected.
- Normal action remains 1 GiB and keeps the encrypted snapshot. Fast file supports metadata up to 1 TiB with a protected original source reference (Windows path / Android persisted document URI). Preparation hashes the original once without a staging encryption/copy. The original must remain unchanged/available; changed content fails final verification. External originals are never deleted by chat clear.
- 100 MiB checkpoint segments, small buffers, encrypted partial storage, segment hashes and final full-file hash. Pending segments only become resumable after verification. Pause keeps completed segments; connection loss retries during the consented running download. Restart requires pressing Download again. Group members can serve complete downloads; metadata-only holders cannot.
- Original file sources seek directly. Completed segmented copies open at the requested segment without decrypting earlier segments. Ordinary encrypted snapshots still decrypt/skip preceding bytes for later segment requests (bounded by their 1 GiB limit). No parallel striping or TLS downgrade.
- Immediate outbox wake with generation tracking prevents the timer race when messages are added during another delivery. Text/seen delivery precedes group sync. Android incoming and outgoing workers are separate. TCP_NODELAY enabled. Buffered Java encrypted reads avoid small disk reads.
- Windows retains keyed cards; statuses/progress update labels, new messages append, and resize width uses outer feed width so scrollbar appearance does not rebuild cards. Double-buffered feed. Group expiry notice has its own row. Notifications remain one per received offer/message, never per segment.
- Both platforms show blue/gray presence dots. File drafts still require Send. Android keeps Send visible beside scrollable attachment actions and stages unknown-size normal files with bounded disk buffers. Preparation blocks duplicate Send actions. Windows snapshot preparation/export runs off the UI thread; synchronous QueueFile wrapper no longer deadlocks on a UI synchronization context.
- Clear/expiry cancels transfers and removes generated snapshot/source-reference/segment storage. Group signatures now required for incoming group frames; relayed metadata additionally validates sender IDs, text and timestamps.

### Changed areas

Windows: PeerEngine.cs, Conversations.cs, new Transfers.cs, Program.cs, version in csproj.
Android: PeerEngine.java, MainActivity.java, MessengerService.java source resolver, manifest versionCode 15/versionName 0.8.0, build output name. Windows About and Android APP_VERSION both 0.8.0.
Tests: linked Transfers.cs into C# harness; explicit download/fast/pause/export/FETCH commands; adapted feature tests for manual consent; new transfers.py and optional large_transfer.py; native Windows card-retention/manual-download regressions. README and PROTOCOL describe current behavior rather than old automatic pushes.

### Verification and actual measurements

Scratch results: C:\Users\user\Documents\Codex\2026-09-20\new-chat-2\work\v08-tests.

- C# harness and standalone Windows release build: 0 errors, 0 warnings.
- Real Java/C# secure integration suite passed: migration, discovery, verification, encryption/tampering, identity spoof rejection, retries/restarts.
- Group/attachment/clear/receipt/relay/expiry/avatar feature suite passed, including multi-chunk ~7 MiB payloads, empty files, corrupt storage and independent download consent.
- transfers.py passed with Java -Xmx64m: 128 MiB fast file in both directions, no sender snapshot, no recipient data before consent, cancellation after the first verified 100 MiB, restart retaining that segment, full hash-checked export, unauthorized/misaligned FETCH, and same-size source mutation rejection.
- Local throttled-transfer text latency: median 0.419 s, max 0.458 s across five messages. Final 128 MiB Windows-to-Java download + encrypted storage + verification: 3.349 s (38.2 MiB/s); an earlier run was 1.893 s (67.6 MiB/s), showing local measurement variability. Java preparation 0.179 s; C# preparation 0.699 s in the final regular run. Resume remaining 28 MiB plus full verification: 1.513 s.
- Optional large_transfer.py passed: **1025 MiB**, C# to Java with a 64 MiB heap, no payload before Download, full integrity check; preparation 2.924 s, download + encrypted storage + complete verification 12.221 s (83.9 MiB/s). Fixture generated locally and deleted after the test.
- Native Windows UI initially caught scrollbar-triggered card rebuilding; fixed and rerun. Its final clear assertion was corrected to select the group being cleared (the newly added manual-download test leaves another conversation selected). ui-final.log ends successfully; exit code 0. Checks cover draft-before-Send, minimized/hidden notifications, seen receipts, existing-card identity across 99 progress callbacks and a new message, explicit Download, and local clear.
- Actual screenshots viewed: compact group with Fast file/expiry notice/blue dots and direct conversation after Download with gray offline dot. Latest successful UI directory: ui-results/ui-d8ea4bdd3c7d48b2a362fa5c495b32cb. Screenshots use synthetic test accounts/1px fixture images, not real photos. Narrow Windows feed can still show a horizontal scrollbar; its appearance no longer causes card reconstruction.
- Android 0.8.0 APK built with the existing key; apksigner verification passed v2/v3. No key regeneration or exposure. Physical Android UI/camera/SAF/Keystore/background behavior was not exercised. All transfer numbers above are loopback on one Windows host, not Wi-Fi/device guarantees.

Logs: final-run.log covers the engine/transfer suites (and the superseded UI clear assertion); ui-final.log is the successful corrected UI run; large-test.log and android-build.log cover the large-file test and APK. No need to rerun unaffected suites solely because the final UI assertion changed.

### Delivery / follow-up

Release outputs are placed only in new-chat-2/outputs with 0.8.0 names: Windows ZIP, APK, source ZIP, release notes, checksums and a Windows preview. .NET Desktop Runtime 9 is required. Install over the existing Android app; update all devices to preserve manual-download semantics. Source archive comes from tracked local Git content and excludes signing keys, build caches and the user's untracked archive.

Remaining acceptance work: real Android/Wi-Fi testing, background/manufacturer behavior, provider-specific seek speed and files beyond 1025 MiB. 1 TiB is a metadata bound, not an exercised size. Fast preparation still makes a full SHA-256 read; receivers retain encrypted private copies before export. TLS remains enabled. There is no parallel multi-stream striping. Download retries while waiting for offline/unavailable sources until Pause. Profile avatars remain automatic, distinct from user-sent image attachments.

## Historical handoff through 0.7.5

The sections below are retained history. The current 0.8.0 section and README/PROTOCOL supersede conflicting old behavior, release paths and “not committed” notes.



## Locations

Source: `C:\Users\user\Documents\Codex\2026-09-20\ig-x20\outputs\lan-messenger` — this is now a real local git repo, see below.

Prior packaged deliverables (through 0.4.2 only): `C:\Users\user\Documents\Codex\2026-09-20\new-chat-2\outputs`. 0.5.0/0.6.0/0.7.0 have not been packaged into that folder (no zip/APK-copy/checksums/release notes there) — see the per-version "continuation" sections below for exactly what has and hasn't been built/verified for each.

0.5.0 added seen receipts and local unread counts. 0.6.0 added group relay and 7-day group expiry. 0.7.0 is a **UI/design pass plus three smaller features**, all explicitly requested by the user: a WhatsApp-inspired redesign (colors, bubble shapes/alignment, per-sender name colors), a local-only profile picture, an About screen with the version number, and the attachment cap raised from 10 MiB to 200 MiB. See "0.7 continuation" for exactly what changed and its real limits (no progress/resumable transfer, avatar not shared with contacts yet).

Windows executable built and verified in source: `windows-dist-v07/LanMessenger.exe` with companion files (`dotnet build`, 0 warnings/0 errors). Android: `LanMessenger-0.7.0.apk` in the repo root, built and signed with the existing development key, `apksigner verify` passed (v2/v3 schemes). No versioned ZIPs, checksums or release notes have been produced for 0.5.0/0.6.0/0.7.0 yet (only the previous 0.4.2 archives exist in the deliverables folder above); package them into that folder if a distributable release is wanted.

**Git: the user explicitly asked (2026-09-22) to work locally with no GitHub — first commit `4122222` was made covering 0.5.0+0.6.0 (root commit; `.packages-cache/` and the stray JVM crash log were excluded from it via .gitignore, per their own earlier note in this file). 0.7.0's changes are on top of that, not yet committed as of this writing — commit them the same way (local only) unless the user says otherwise.** No remote is configured; do not add one or push without being asked again — a prior approval does not carry forward to a new remote/push action.

## Current implementation

Windows C# / WinForms / .NET 9; Android Java and native Activity/foreground service. English UI. No host, cloud or accounts. UDP 43871 discovery; TCP 43872 TLS; LM4 protocol. Saved UUIDs, contacts, histories, Queued/Delivered/Seen statuses plus local-only Read/unread tracking. For 1:1, both devices must be reachable together for delivery — unchanged. For groups (0.6.0+), a message can now also reach a member through another already-verified member if the original sender is offline, and expires 168h after it was sent. WhatsApp-style UI (0.7.0): green accent, dark teal header, beige chat background, rounded content-sized bubbles aligned by sender, hashed per-sender name colors, local profile picture, About, 200 MiB attachment cap.

0.3 implements mutual-certificate TLS 1.2, stable RSA identities, explicit matching safety-code verification, pinned certificate checks and changed-key rejection. Windows TLS uses BouncyCastle.Cryptography 2.7.0; Android uses platform JSSE. Local primary/backup history and identity are protected by DPAPI on Windows and Android Keystore AES-GCM on Android. Migration preserves 0.2 records; there is no downgrade support.

Windows tray notifications contain sender name and a generic new-message label. Close hides to tray; explicit Exit stops reception. Notification click restores the sender conversation. Tests cover minimized and hidden reception. This continuation fixed rendering before notification-driven selection, isolated optional sound errors, added reproducible UI regression coverage and prevented accidental signing-key regeneration.

## Important files

- `windows/Program.cs`: Windows UI, tray and notification routing; 0.7.0 color palette (`Accent`/`HeaderDark`/`Ink`/`BubbleMine`/`BubbleOther`/`ChatBg`/`SeenBlue`/`NamePalette`), `RoundCorners` helper, avatar UI, About dialog.
- `android/src/net/lanmsg/chat/MainActivity.java`: same 0.7.0 UI work on Android (color fields, `circle`/`circleBg` avatar helpers, `nameColor`, bubble alignment via a weighted-spacer row, About/avatar).
- `windows/PeerEngine.cs`, `android/src/net/lanmsg/chat/PeerEngine.java`: discovery, pinning, transport, persistence and queues; keep both compatible. `Avatar`/`SetAvatar` (`avatar()`/`setAvatar()`) added 0.7.0 — local-only, not part of the wire protocol, stored like identity/attachments (`avatar.sec`).
- `windows/SecureChannel.cs`: Bouncy Castle TLS adapter; now also exposes the peer's public key (`PeerPublicKey`), not just its fingerprint.
- `windows/SecureIdentity.cs`: certificate identity, DPAPI and protector boundary; now also `Sign`/`Verify` using the same RSA key, for group-message signatures.
- `android/src/net/lanmsg/chat/SecureIdentity.java`, `AndroidProtector.java`: JSSE identity and Keystore storage; same `sign`/`verify`/`remotePublicKey` addition.
- `android/src/net/lanmsg/chat/MessengerService.java`: Android service/alerts.
- `tests/integration.py`: Java/C# interoperability and security regressions.
- `tests/WindowsUi`: native WinForms notification and composition test; invokes the actual NotifyIcon click event handler by reflection, not a physical desktop click.
- `tests/run.ps1`: reproducible local build and automated checks.

## Build environment

JDK: `C:\Program Files (x86)\Android\openjdk\jdk-17.0.14`

SDK: `C:\Program Files (x86)\Android\android-sdk` (platform 34, build-tools 35.0.0).

Private existing signing key: `C:\Users\user\Documents\Codex\2026-09-20\ig-x20\work\android-build\development.keystore`. Keep private, do not commit/archive, do not regenerate. Package ID remains `net.lanmsg.chat`; versionCode 9 / versionName 0.7.0. APK uses existing development signing credentials, not a production signing setup. `android:largeHeap="true"` was added in the manifest for 0.7.0 to give the raised 200 MiB attachment cap headroom against OOM on real devices.

Use `android/build.ps1`; preserve its default BuildRoot or privately copy the original key to a supplied BuildRoot. A missing key now fails unless `-GenerateDevelopmentKey` is explicitly passed for a new development installation.

If the restricted build account cannot access NuGet configuration, set process-local DOTNET_CLI_HOME and APPDATA under the existing `ig-x20/work/dotnet` and `ig-x20/work/appdata`. Do not change user-wide settings.

## Verification and remaining work

Windows Release build: no warnings/errors. APK built, signature verified; signing certificate continuity and metadata checked. Automated engine tests cover migration of saved history and both outgoing queues, discovery, safety codes, unverified blocks, Unicode/ACK, wrong recipients, duplicate messages/notifications, captured encrypted traffic, modified ciphertext, impersonated IDs, changed keys, restart/reconnect and Java-to-Java. Native Windows tests cover minimized/hidden notifications, generic content, conversation routing, no repeated alerts, offline composing and tray exit. UI screenshot reviewed.

0.5.0 re-ran the full suite (`tests/run.ps1`) after the seen/unread changes and it passed end to end, including: unread count increments on arrival and clears on read, a 1:1 seen receipt reaches the original sender, a group's aggregate status stays Delivered until every member has read it and only then becomes Seen, and (native Windows UI) opening a conversation marks it read and the feed actually renders the ✓/✓✓ ticks after a real Seen round-trip between two live engine instances.

0.6.0 re-ran the full suite again after the group-relay/expiry changes and it passed end to end, including every prior test (0.2/0.3/0.4/0.5 regressions, 1:1 messaging fully unaffected) plus new coverage: a 4-member group where the original sender goes offline after sending a message and an attachment, and a returning member receives both, correctly attributed, from a different member; that same content arriving from two different relaying members at once does not duplicate; a group message with an already-expired timestamp is rejected on arrival and never stored, even from an otherwise-trusted member; and an already-expired message seeded directly into a device's own storage is purged at startup before any network activity. This caught one real pre-existing bug in the process (see "0.6 continuation") and one real test-harness regression, both fixed and re-verified.

0.7.0 re-ran the full suite again after the UI/design/avatar/limit changes — no protocol or storage-format change this round (avatar storage is a new file, not a new frame/row shape), so this run is mainly a regression check, and it passed end to end with no changes needed. The native Windows UI test additionally reflects the real new design: the `windows.png`/`windows-seen.png` screenshots it produces were inspected and show the dark teal header, green accents, colored per-sender names (visibly different color from the message body), and content-sized/aligned bubbles with the blue ✓✓ Seen tick — i.e. the redesign was visually verified, not just compiled. Android was compile-checked (`javac` against the SDK, 0 errors) but not screenshotted — there is no automated or manual visual check of the Android UI in this pass. `dotnet build windows/LanMessenger.csproj -c Release -o windows-dist-v07` and `android/build.ps1` were both run standalone (not just inside the test harness) and succeeded.

Harness data is isolated; test storage protectors are used instead of real DPAPI/Android Keystore. No physical Android device test and no proof of visible Windows banners under the user's desktop settings. The release is ready for user acceptance testing, not a fully hardware-validated production release.

Manual acceptance: update ALL devices in any group together this time (0.6.0+ group messaging is not backward-compatible with 0.4.x/0.5.0 — see "0.6 continuation"), without uninstalling; compare/verify codes; exchange Arabic/emoji both ways; test Windows hidden/minimized and click a real banner; test Android screen-off/background reception; disconnect/reconnect Wi-Fi and confirm queued messages deliver once after restart. Check that old conversations remain. For 0.5: confirm the unread badge appears on an unopened conversation and clears on opening it on a physical device, confirm your own sent message actually turns blue/double-tick once the other real device opens the chat (not just when it's delivered), and confirm a group only shows Seen once every real member has opened it. For 0.6: with a real 3-device group, turn one device's Wi-Fi off, send it a message + photo from a second device, then bring the third device back online near the second (not the first) and confirm it receives both correctly attributed to the original sender; confirm the "deleted after 7 days" notice shows inside a real group. For 0.7: look at the actual Android screen (this build has never been visually checked there) — confirm colors/bubbles render sanely on a real device/screen size, set a profile picture and confirm it persists after restart, and test a genuinely large (~150–200 MiB) transfer over real Wi-Fi on a phone to see whether the raised cap is actually usable in practice or too slow/memory-heavy without the progress/resume work that was explicitly deferred. Do not change firewall/router settings or install on devices without authorization.

The user explicitly authorized UI improvements, local clear, files/photos and groups for 0.4, explicitly asked for seen receipts and unread counts "like WhatsApp" for 0.5, explicitly asked (in Arabic, with a detailed written spec) for group message relay through other members plus 7-day group expiry for 0.6, and explicitly asked for a WhatsApp-like redesign, a local profile picture, an About screen and a 200 MiB attachment cap for 0.7 — each supersedes the earlier "read receipts/this remains outside scope" notes below where they conflict; those were accurate only as of when written. Keep all of these features and preserve all user data/signing identity. Cloud sync and a central server/blockchain remain explicitly out of scope (the user repeated the no-server constraint specifically for 0.6). For 0.7 the user was explicitly asked and chose: profile picture local-only (not shared with contacts — that's a real follow-up feature, not done), and "just raise the number" for the file limit over the fuller progress/resumable-transfer rework (also a real follow-up, not done) — don't add either without the user asking again, since they were offered and declined this round.

## 0.4 continuation

Requested features implemented: refreshed UI on both platforms, confirmed local-only clear, encrypted image/file attachments up to 10 MiB with save/preview, and fixed 3–16-member group creation and direct fanout. User explicitly chose local-only clearing. No remote deletion. Membership editing/leave is outside this iteration; new groups use selected verified contacts, and every pair must verify for messaging.

New Windows `Conversations.cs` contains groups, local clearing/tombstones and encrypted attachment storage; CsharpHarness links it explicitly. Java equivalents remain inside PeerEngine.java. Extended M rows, G group rows and H cleared-ID rows use LMSTORE4 inside the unchanged protected envelope. LM4 requires upgrading all peers. Existing 0.3 identity/certificate and code derivation are intentionally unchanged.

`tests/features.py` exercises Java and C# group creation/replies, fanout/ACK aggregation, offline members/restart, photo/binary/empty attachments, size/path/hash rejection, unauthorized members and group replacement, encrypted-blob tampering, clear isolation, duplicate suppression after clear and queue cancellation. `tests/migration_v03.py` successfully ran actual retained 0.3 harnesses from ig-x20/work/peer-tests and upgraded their data with the new harnesses, preserving pins, identity, conversation and pending delivery without re-verification.

Scratch builds/results: ig-x20/work/v041-draft-tests and v041-draft-final-results. Final deliverables are in new-chat-2/outputs with 0.4.2 names. The native UI test covers both default and minimum window sizes, group selection, explicit attachment drafts, captions, inline image rendering and attachment controls. Android compiles and retains the signing certificate, but physical camera/picker/export/preview/background testing remains required. No phone installation, firewall changes, commits or upload were performed. The previous upload request still needs a repository URL.

## 0.5 continuation

User asked directly, in English, for "seen" receipts and a per-chat unread count "like WhatsApp." Implemented as a new additive `LM4 SEEN`/`SEENACK` frame pair (see PROTOCOL.md "Seen receipts") sent asynchronously from the recipient's own periodic peer loop once the local user actually opens the conversation — never inline with ACK, since delivery and reading are genuinely separate events in time. No storage-format change: the existing per-message Status string just gained two more values, reused symmetrically for both directions — `Received`→`Read`→`Seen` on the reader's own incoming row (this is also exactly what "unread" counts, i.e. rows still `Received`), and `Delivered`→`Seen` on the sender's own outgoing row once a valid SEEN frame arrives for it. A group's aggregate status (already an existing concept for Delivered) got a third tier the same way: Seen only once every per-recipient row is Seen.

Marking read is a UI action, not a protocol one: both `Program.cs` (`Render()`) and `MainActivity.java` (`render()`) call the engine's new `MarkRead(conversation)` at the top of their existing per-tick render, guarded on Windows by `Visible && WindowState != Minimized` (Android already only renders while the Activity is resumed) — so a conversation left open counts as continuously "being read," matching a normal chat app, and a backgrounded/minimized/hidden window does not silently mark things read the way the existing minimized/hidden-notification tests already assume. Own-message bubbles now show ✓/✓✓ ticks (blue once Seen) instead of a bare status word; incoming bubbles show no status caption at all (a reader was never shown their own read-confirmation state before, and still isn't — only the sender sees ticks, matching WhatsApp). The conversation list gained a small unread-count badge (green circle, WhatsApp-style) per contact/group, computed from the new `Unread(conversation)`/`unread(conversation)` engine call.

Wire-compatible with 0.4.x on purpose (unlike the LM3→LM4 break): an unrecognized `SEEN` frame just makes the older peer close the connection, which the existing per-frame exception handling in `Receive()`/`receive()` already tolerates — verified by re-running the full suite, not just reasoned about. `tests/PeerHarness.java` and `tests/CsharpHarness/Program.cs` both gained matching `READ`/`UNREAD` commands so `tests/features.py` could drive this from Python like every other engine feature; the new assertions there cover the 1:1 round trip, unread clearing, and the group Delivered-until-all-Seen tiering, all against the real Java and C# engines over real loopback TLS. `tests/WindowsUi/Program.cs` got one new block (reusing the existing `second` peer and the existing `RestoreWindow` reflection helper) that drives a real send from the actual composer/Send button through to a real Seen confirmation and then asserts the literal "✓✓" appears in the rendered feed — this caught two direction mistakes (checking the wrong side's message row) during development, which is exactly the kind of bug this level of test would be expected to catch.

`windows-dist-v05/LanMessenger.exe` and `LanMessenger-0.5.0.apk` (versionCode 7, existing signing key, `apksigner verify` OK) were built standalone in source; superseded by 0.6.0 below. Nothing was committed, pushed, or installed on a device.

## 0.6 continuation

User asked (Arabic, detailed spec) for exactly four things, group-only, with explicit boundaries: (1) a member who receives a group message keeps a local copy it can forward to a fellow member who missed it; (2) a reconnecting member retrieves what it's missing from whoever's reachable, even if the original sender is offline, switching source if one drops; (3) a stable per-message ID prevents duplicate storage/display and the original sender's identity must be preserved; (4) a group message and its attachment expire 168h after their *original* send — re-forwarding never renews it — purged periodically and at startup before syncing, and rejected on receipt if already expired so an old device can't resurrect one. Explicit non-goals: don't touch 1:1 at all, no central server/blockchain, don't redesign membership/encryption/UI, don't retroactively delete old history.

The one design decision that needed real thought: (3) "preserve the original sender's identity" through a relay is a *security* requirement, not just a data-plumbing one. Before 0.6.0, a message's sender was authenticated purely by the live TLS connection it arrived on — fine for direct delivery, but if member B just forwarded A's bytes to C, C would be trusting *B's* connection for content claiming to be *A's*. That's exactly the "third-party relay with implicit trust" PROTOCOL.md already documented as explicitly ruled out (`No implicit certificate trust or third-party relay`), so a naive relay would have been a real, silent security regression — a compromised or malicious relaying member could otherwise put words in someone else's mouth. Fix: reuse the *existing* per-device RSA identity (already generated once per install for TLS, see Encrypted direct transport) to sign each group message's content; a relayed message is only trusted if the recipient has *already independently verified the original sender's own device* — receiving something via a relay never substitutes for that. No new key material, no group-shared secret, no redesign of membership or encryption — see PROTOCOL.md "Group relay and 7-day expiry (0.6.0)" for the exact frame formats and validation rules.

Wire/storage: `P` rows gained an 8th field (the peer's public key, captured at the same already-authenticated TLS handshake point fingerprints already come from — no new exchange). `M` rows gained two fields (signature, and a TTL-eligible flag) — old rows without them load as not-eligible, so **nothing existing is retroactively expired**, satisfying that boundary directly; new fields are populated only for group messages, so 1:1 rows are byte-for-byte unaffected. Two new frame types, both group-only and both requiring an already-`Trusted` connection like every other frame: `SYNCREQ`/`RELAY`/`RELAYACK`/`SYNCDONE` for pull-based sync (the *requester* always initiates, each cycle, against every reachable verified fellow member — this is what makes "C comes back and pulls from B" work with no special-casing for who reconnected first), and direct group `MSG` frames grow one optional field (the signature) *only when a group ID is present* — 1:1 `MSG` frames are still exactly 8 or 12 fields, unchanged. This does mean **group messaging itself is not backward-compatible with 0.4.x/0.5.0** (an old peer doesn't recognize the longer frame and just drops the connection, the same harmless way an unrecognized `SEEN` frame already does) — unlike Seen receipts, every member of a group needs 0.6.0 to keep exchanging group messages at all; 1:1 is completely unaffected regardless of version on either end.

Implementation is symmetric across `windows/PeerEngine.cs`/`android/.../PeerEngine.java` as always, plus `windows/SecureIdentity.cs`+`SecureChannel.cs`/`android/.../SecureIdentity.java` for signing and public-key capture, plus one persistent UI notice ("Group messages and their attachments are automatically deleted after 7 days of being sent") shown inside every group on both platforms. `tests/PeerHarness.java` gained a `RAWTIME` command (explicit-timestamp raw group frame, for expiry testing) and `tests/features.py` gained the four scenarios the user asked to be verified by, all against real Java/C# engines over real loopback TLS.

Two real bugs were caught and fixed during this work, both worth knowing about: (a) `Remember()`/`remember()` — called on *every* UDP discovery packet, i.e. every 2–3 seconds — rebuilt each `Peer` from scratch and, unlike fingerprint/verified which it already carried forward, silently dropped the newly-added public key back to empty each time; it would have self-healed on the next TLS handshake but left signature verification transiently and intermittently failing in between. Fixed on both platforms by threading the old public key through like the other fields. (b) The pre-existing `RAW` test harness command hardcoded a fixed, already-ancient timestamp (`1700000000000`) for constructing test MSG frames — harmless before 0.6.0, but it made one *existing, unrelated* test (duplicate-retry-after-clear) start failing once real expiry checking existed, since that timestamp is now genuinely expired for a real group. Fixed by making `RAW` use the actual current time by default and adding the separate `RAWTIME` command (explicit timestamp) for tests that specifically want an old one, like the new expiry tests — no existing call site needed to change.

`windows-dist-v06/LanMessenger.exe` and `LanMessenger-0.6.0.apk` (versionCode 8, existing signing key, `apksigner verify` OK, actual attachment storage path unchanged: `attachments/<original-sender-uuid>-<message-uuid>.sec` under the same per-install data directory as before) were built standalone in source; superseded by 0.7.0 below. `android/build.ps1`'s hardcoded output filename was bumped for both 0.5.0 and 0.6.0 in turn (it's easy to miss — it doesn't derive from the manifest version).

## 0.7 continuation

User asked for four things in one request: (1) "work on the design, colors, the UI interface is bad, make it similar to WhatsApp" — general; (2) About with the version number; (3) profile picture; (4) raise the 10 MiB attachment limit to at least 200 MiB. Two of these were ambiguous enough in scope/cost to ask about rather than guess, and the user answered both directly: profile picture **local-only** (not sent to contacts — sharing it is a real, separate follow-up feature, not started), and the file limit **"just raise the number"** over the fuller "make large transfers actually reliable" option (progress bar, pause/resume, streaming to disk — also a real, separate follow-up, not started). Respect both of those boundaries unless the user asks again; they were explicitly offered and declined this round, not overlooked.

Design: WhatsApp-inspired palette applied consistently on both platforms — dark teal header (`#075E54`), green accent (`#25D366`-family) for buttons/links/unread badges, light-green outgoing bubbles (`#DCF8C6`-family) vs white incoming, beige chat background (`#ECE5DD`-family), and blue (`#53A9EF`-family) kept *only* for the Seen tick, matching real WhatsApp's one deliberate break from its own green theme. Per-sender name color is a small fixed palette (7 colors) selected by hashing the sender's device ID, so the same person is always the same color across the whole app, distinct from both the green "you" and the black/near-black message body — this directly addresses the "sender name style/color different from the chat content" part of the request, which the pre-existing code only did with bold weight, not color, for incoming messages. Windows bubbles are now genuinely rounded (`RoundCorners`, a `GraphicsPath`-clipped `Region` reapplied on resize — WinForms has no built-in rounded-panel support) and were already correctly content-sized/aligned by the prior session's code; Android bubbles were already rounded (`GradientDrawable` corner radius, pre-existing) but were NOT properly sized/aligned before this pass — they stretched close to full width with only a margin offset, not a real bounded bubble hugging its own content pinned to one side. Fixed by wrapping each message in a row with a weighted empty spacer on the non-bubble side (the standard Android trick for "align to one edge, size to content") and capping text views' width via `setMaxWidth`. This was a real, pre-existing correctness gap on Android, not just a color change.

Profile picture: `PeerEngine.Avatar`/`SetAvatar` (`avatar()`/`setAvatar()`) store/read a single encrypted `avatar.sec` file next to `state.txt`/`identity.sec`, same protector, same atomic-write pattern as everywhere else — genuinely local-only, never touches the wire protocol, so no version-compatibility concern at all (unlike 0.6.0's group changes). Windows: a round clickable avatar in the toolbar, falls back to a colored initial when unset. Android: same in the People toolbar, plus reuses the existing image-decode/subsample helper (`inlineBitmap`) to keep the stored copy small.

File limit: `MaxFileSize`/`MAX_FILE_SIZE` raised from 10 MiB to 200 MiB on both platforms. This alone would have made the feature non-functional at real LAN speeds, so — while staying inside "just raise the number," not the declined reliability rework — the previously-hardcoded 60-second all-or-nothing network timeout (for both the read and write side of an attachment transfer) was changed to scale with size (`max(60s, 30s + 1s/MB)`, ~230s at 200 MiB) on both platforms; without this, the raised cap would fail on essentially any real transfer above a few hundred MB/60s. `android:largeHeap="true"` was added to the manifest since the existing code reads/encrypts/sends the whole file in memory at once (unchanged, no chunking added) and 200 MiB would very likely OOM on a stock Android heap otherwise. None of this adds a progress indicator, cancellation, resumability, or streaming disk I/O — a large transfer still looks "frozen" to the user until it completes or times out, exactly as flagged when this was offered and declined.

Verification: full `tests/run.ps1` suite re-run, passed unchanged (no protocol/storage-format change this round, so this is a regression check, not new scenario coverage) — see "Verification and remaining work" above for what was *visually* checked (Windows, via screenshot) versus not (Android, compile-only). `windows-dist-v07/LanMessenger.exe` and `LanMessenger-0.7.0.apk` (versionCode 9, existing signing key, `apksigner verify` OK) were built standalone in source. Nothing was committed as part of writing this section — see the Git line under Locations above for the actual commit state, which does need to be re-checked/updated before trusting anything in this paragraph about what's committed.

### 0.7.1 (Android only — real user acceptance testing on a physical device caught what compile-only checking couldn't)

User reported, in Arabic, three concrete Android problems after trying 0.7.0 for real: (1) the profile-picture tap did nothing; (2) the software keyboard covering most of the chat while typing, with no way to reclaim space; (3) no auto-scroll to the latest message when the keyboard opens. All three are Android-only per the user; Windows is untouched, still versioned 0.7.0.

(1) was a real bug in the 0.7.0 code, not a device-specific quirk: `showPeople()` built a clickable `ImageView` (`avatarView`) with the change-picture listener, but only ever *added it to the screen* in the branch where an avatar image already existed; the no-avatar-yet fallback (everyone's default state) added a *different*, non-clickable `TextView` showing the initial letter instead. So the visible circle had no listener, and the listener lived on an element that was never shown — this is exactly the kind of bug that compiling cleanly and even a code read can miss, and is why the HANDOFF note above calling this "done" for Android was wrong. Fixed by wrapping both the image and the initial-letter fallback in one `FrameLayout` that is always added and always carries the click listener, regardless of which child is currently visible.

(2)/(3) are new, driven by one shared mechanism: `showChat()` now attaches a `ViewTreeObserver.OnGlobalLayoutListener` to the window's decor view (the standard Android technique for detecting the soft keyboard, since there's no direct callback for it) that compares the window's visible frame height to the full window height; when the gap exceeds ~15% of screen height, the keyboard is inferred to be open. On the open transition it sets `Verify device`/`More` (and the group-expiry notice, if present) to `GONE` — leaving just the back arrow, per the user's literal ask — and scrolls the feed to the bottom once; on close it restores them. The listener is added at the end of `showChat()` and explicitly removed at the top of `frame()` (which both `showPeople()`/`showChat()` call first) so it never accumulates duplicates across navigation. This is a heuristic (screen-height-ratio), not an official API, but it is the standard approach absent `WindowInsets` APIs this codebase doesn't otherwise use.

Verified: full `tests/run.ps1` still passes (these are Android-UI-only changes, invisible to the engine/Windows-UI test suite — expected, not meaningful new coverage) and `javac` compiles clean; **there is still no automated or manual visual/interactive check of the Android UI in this repo** — everything Android-UI-shaped, including whether this specific fix actually resolves what the user saw, is going on their direct report and code reasoning, not a screenshot or device test the way Windows gets. `LanMessenger-0.7.1.apk` (versionCode 10, existing signing key, `apksigner verify` OK) built standalone. If another Android UI issue gets reported, treat it as equally plausible that it's a real bug like this one, not user error — this codebase's Android UI has now demonstrably shipped with at least one dead control.

### 0.7.2 (Android only) — chat-screen space and mini avatars

User followed up (Arabic) still on 0.7.1: the chat still felt cramped because the "LAN Messenger" app-title bar, a separate button row (back/Verify device/More), and a separate contact-name row were all stacked on top of each other, and asked for the chosen profile picture to show next to the sender's name in the chat, small.

Space: `frame()` no longer builds a header at all — it now only sets up the padded `content` area and exposes the *outer* container as a new field, `chrome` (full-bleed, holds the header + the padded content). `showPeople()` still builds the full "LAN Messenger" branded bar and inserts it at `chrome` index 0 (unchanged look on the contacts screen — the user's complaint was specifically about the chat screen). `showChat()` now builds one compact bar instead: back arrow, a small contact/group avatar circle (colored initial, group tint is a fixed light purple, peer tint is the same per-sender hash used elsewhere), the name+status (`heading`, now a plain white bold single-line ellipsized `TextView` built directly rather than via the `label()` helper, to avoid its default padding), and an overflow button (⋮). "Verify device" moved from a standalone button into that overflow menu alongside "Group members"/"Clear conversation" (`chatMenu()`) — it only appears there for a 1:1 peer, not a group, same logic the button's old `verifyButton.setEnabled(peer!=null)` used to express; that field is gone now, nothing else referenced it. Net effect: three stacked rows become one, and it's permanent (not just while the keyboard is open, unlike the 0.7.1 keyboard-hide behavior which still separately hides the group-expiry notice and auto-scrolls).

Mini avatars: each message bubble's sender-name row (`whoRow`) now has an 18dp circle before the name — the sending device's own decoded `avatar()` bitmap for `mine` messages when set (decoded once per `render()` call via `PeerEngine.avatar()`/`inlineBitmap`, shared across every own-message bubble in that render pass — `releaseImages` already null-checks `isRecycled()` before recycling so reusing one Bitmap instance across several ImageViews is safe), otherwise a colored-initial circle (same `nameColor`/`circle` helpers as everywhere else) for both un-photographed "you" and for every contact, since contacts' photos are still not shared over the network — this remains a purely local, per-device feature exactly as scoped in 0.7.0's continuation.

Verified: full `tests/run.ps1` passes (Android-UI-only again, not exercised by the suite) and `javac` compiles clean. `LanMessenger-0.7.2.apk` (versionCode 11, existing signing key, `apksigner verify` OK) built standalone. Same caveat as 0.7.1: no visual/interactive check of the Android UI exists in this repo or process — this is reasoned from the code and the user's own description, not observed rendering. If the user reports the header still looks wrong or the avatars don't show, treat it as plausible rather than assuming it's already correct.

### 0.7.1 (Windows) — mini avatar next to the sender name, brought over from Android 0.7.2

User asked directly ("هل نسخة البي سي تعرض الصورة في المحادثة؟" / "نعم ضيفها للبي سي") whether the Windows build showed the profile picture in chat the way Android 0.7.2 now does. It didn't — checked first (`grep` for `avatarBox`/`avatarImage`/`MessageCard` in `windows/Program.cs`) and confirmed the avatar was only ever drawn in the toolbar — then added it on request.

Added `DrawInitialCircle(initial, color, size)` (a small `Bitmap` drawn with `Graphics.FillEllipse` + centered `TextRenderer.DrawText`, antialiased) and `SenderRow(mine, name, color, width)`, which builds a `FlowLayoutPanel` holding an 18px `PictureBox` followed by the existing name `MessageLabel`. `MessageCard()` now calls `SenderRow(...)` instead of adding the name label directly. For `mine` messages with a set avatar, the `PictureBox` reuses the toolbar's already-cached `avatarImage` `Bitmap` directly (safe to share across many `PictureBox`es without double-free — confirmed by the existing codebase's own pattern of only explicitly disposing *freshly drawn, per-instance* bitmaps, e.g. `picture.Disposed+=(_,_)=>thumbnail.Dispose();` for attachment thumbnails; `PictureBox.Dispose()` never disposes a shared `.Image` on its own). For everyone else (and for "you" with no avatar set), a fresh `DrawInitialCircle` bitmap is drawn per bubble and explicitly disposed via `avatar.Disposed+=(_,_)=>drawn.Dispose();`, matching that same existing pattern, so this doesn't leak GDI handles.

No protocol/storage change — purely a rendering addition, mirrors Android exactly in scope (own real photo if set, colored initial otherwise, never a contact's photo since avatars are still local-only).

Verified: full `tests/run.ps1` passed, including the native `WindowsUi` reflection test. Unlike every Android UI change this session, this one *was* visually confirmed — read the actual `windows-seen.png` screenshot it produced (`ui-results/ui-d13def4383554a4c8416858e7ccb5708/windows-seen.png`) and saw a green "Y" circle next to "You" and an orange-toned "D" circle next to "Design team", both rendering correctly next to their names in the bubbles. `windows/LanMessenger.csproj` `<Version>` and `Program.cs`'s `AppVersion` constant bumped `0.7.0`→`0.7.1`; `windows-dist-v071/LanMessenger.exe` built standalone (old `windows-dist-v07` folder removed). Android stays at 0.7.2, unaffected — this round touched Windows only.

### 0.7.2 (Windows) — crash fix for the 0.7.1 mini-avatar feature

User ran the actual `windows-dist-v071` build and hit a real crash, reported as a raw .NET unhandled-exception dialog: `System.ArgumentException: Parameter is not valid` inside `PictureBox.ImageRectangleFromSizeMode` during `OnPaint`. That exact message is GDI+'s signature error for "this `Image`/`Bitmap` object is disposed or otherwise invalid" — not a sizing or layout bug.

Root cause: 0.7.1's `SenderRow()` pointed a message bubble's mini-avatar `PictureBox.Image` directly at the shared toolbar `avatarImage` field for every "mine" bubble (`avatar.Image=avatarImage`, no copy). `ChangeAvatar()` (existing code, line ~239) does `avatarImage?.Dispose(); avatarImage=thumb;` whenever you pick a new photo — correct for the toolbar's own `PictureBox`, which gets its `.Image` reassigned in the same call, but every *already-rendered message bubble* from earlier in the conversation still held a reference to that same now-disposed `Bitmap` instance. The next time any of those older bubbles repainted (scrolling, window resize, restore-from-minimize), GDI+ tried to read size/pixel data from a disposed native handle and threw. This is exactly the class of shared-mutable-resource bug the codebase's own existing pattern (dispose only *freshly drawn, per-instance* bitmaps, e.g. the attachment-thumbnail `picture.Disposed+=(_,_)=>thumbnail.Dispose();` pattern already in this file) was designed to avoid — 0.7.1's avatar branch didn't follow it and shared the cached instance directly instead.

Fix: `SenderRow()` now clones the avatar (`new Bitmap(avatarImage)`) per bubble when `mine&&avatarImage!=null`, and disposes that clone on the `PictureBox`'s own `Disposed` event — identical lifecycle to the initial-letter fallback branch right next to it. Each bubble now owns an independent copy; changing your photo later disposes only the shared toolbar source, never anything a bubble is still painting from. One-line-pattern fix, no behavior change otherwise.

Verified: full `tests/run.ps1` re-run, passed including the native `WindowsUi` screenshot test; re-read the resulting `windows-seen.png` and confirmed the avatars still render correctly after the fix. `windows/LanMessenger.csproj`/`AppVersion` bumped `0.7.1`→`0.7.2` (Windows) — deliberately re-versioned rather than silently patched, since a real broken build (0.7.1) had already reached the user. `windows-dist-v072/LanMessenger.exe` built standalone (old `windows-dist-v071` folder removed). Android unaffected, unchanged, stays 0.7.2 (coincidentally the same number as Windows now — the two platforms version independently, this is not a synchronized release).

### 0.7.3 (both platforms) — profile pictures now actually shared with the other side

User pointed out that after the previous avatar work, the picture only ever showed on your own device — the other person never saw it — and asked for it to actually be sent. This is the feature that was explicitly offered and declined back in 0.7.0 ("local only for now"); the user has now asked for it, so it's implemented here. Bumped both platforms together (`0.7.2`→`0.7.3`) since this is a wire-protocol change, not a single-platform UI tweak like the last several rounds.

Design mirrors Seen receipts, not group relay: an avatar travels directly between two devices that have already verified each other — never through a third party — so unlike group messages it needs no signature, just the same TLS mutual-authentication and Trust gate everything else already uses. New persisted `Peer` fields `SentAvatarHash`/`ReceivedAvatarHash` (C# `PeerEngine.cs`, mirrored as `sentAvatarHash`/`receivedAvatarHash` in Java `PeerEngine.java`) track, per peer, the hash of the avatar we last successfully pushed to them and the hash of the avatar we last received from them — the same "row grows, old rows default to empty" backward-compatible pattern used for every previous P-row extension (now 10 tab-separated fields; older 5/7/8-field rows still load fine). New wire frames `LM4 AVATAR <sender-id> <hash> <byte-length>` (header, then raw bytes if length>0) and `LM4 AVATARACK <hash>`, dispatched in `Receive()`/`receive()` alongside the existing SEEN/SYNCREQ cases, and pushed from `Deliver()`/`deliver()`'s per-peer loop — the same "compare local state to last-acknowledged state, push on mismatch, only advance local state after a valid ACK" pattern SEEN already uses, so a dropped connection just retries next ~2s tick like everything else in that loop. A push only happens `if(peer.Trusted)`/`if(p.trusted())`; an unverified or not-yet-verified peer never receives anyone's picture. A received avatar is capped at 2 MB (`MaxAvatarSize`/`MAX_AVATAR_SIZE`) and hash-checked before being trusted, then stored the same way as the local avatar and every attachment: an encrypted per-peer file, `avatars/<peer-device-id>.sec`, decrypted with the same storage protector. Clearing your own avatar (hash becomes `""`) propagates the same way — the peer deletes its stored copy of your picture.

UI: both platforms' message-bubble mini-avatar (added in 0.7.1 Windows / 0.7.2 Android) now checks for a real received avatar (`engine.PeerAvatar(senderId)` / `e.peerAvatar(m.from)`) before falling back to the colored initial, for the non-"mine" case. On Windows this decodes a fresh `Image` per bubble via the existing `TryImageThumbnail` helper and disposes it on that `PictureBox`'s own `Disposed` event — deliberately NOT sharing a cached instance across bubbles, learning directly from the 0.7.2 crash fix above. On Android this reuses the existing `inlineBitmap` decode-per-render-pass pattern (already proven safe, see the 0.7.2 Android continuation) — no new disposal risk there since Android already recycles every bitmap in the feed tree together at the start of the next `render()` call. Deliberately did not touch the contact-list circle or the chat-header avatar on either platform — matching the literal ask ("the picture doesn't reach the other side in messages") and keeping scope proportionate; can be extended later if asked.

Verification: added `SETAVATAR`/`CLEARAVATAR`/`PEERAVATAR` commands to both test harnesses (`tests/PeerHarness.java`, `tests/CsharpHarness/Program.cs`) and a new scenario in `tests/features.py` — one verified device sets an avatar, the other side receives it (hash+length match), then the first clears it and the other side's copy disappears too. First attempt at this test used peer `a`, which — as the surrounding test file already documents — gets deliberately stopped earlier in the same run for an unrelated offline-sender relay test and is never restarted; that made the harness process's stdin pipe closed by the time my new code ran, producing a Python `OSError` that looked at first like a real product bug. Re-read the file, confirmed `a` is intentionally dead at that point, and switched the test to use `b`/`c` (verified with each other from the start of the file, never stopped) instead — real test-authoring mistake, not an app bug, same category flagged elsewhere in this document. Full suite passes after that fix, including this new scenario; Windows UI re-confirmed via a fresh `windows-seen.png` (no avatar set in that specific scripted scenario, so it correctly still shows colored-initial circles — this run didn't visually exercise a real received photo, only the protocol delivery, which the Python test scenario does check directly via hash/length). `windows-dist-v073/LanMessenger.exe` and `LanMessenger-0.7.3.apk` (versionCode 12, existing signing key, `apksigner verify` OK) built standalone. `PROTOCOL.md` gained an "Avatar sharing (0.7.3)" section documenting the exact frame format and trust rationale.

### 0.7.4 (both platforms) — attachments up to 1 GiB, streamed instead of loaded whole into memory

User asked why the current transfer system hangs on large files. Traced it precisely (not guessed): `Send()`'s click handler (both platforms) called the engine's queue method directly and synchronously on the UI/main thread, which does hashing + at-rest encryption + a full local-state rewrite before returning — for a large file this really freezes the UI (not just "feels slow"), and on Android risks a genuine ANR. Explained this, then the user asked for a real design to raise the cap to 1 GiB, explicitly agreeing to trade away per-chunk at-rest authentication for the ability to stream (see PROTOCOL.md's new "Streamed attachments" section for the exact design) — a real, deliberate security-posture decision made with the user, not something decided unilaterally.

This was the single largest, riskiest change of the session — a full rewrite of the attachment pipeline on both platforms (local storage format, network send/receive, group relay send/receive, the Windows and Android UI attach/draft/send/export/preview flows) — and it surfaced **six real, independent bugs**, five of them only found by actually running the full test suite repeatedly and chasing failures to ground truth rather than guessing:

1. **Java `CipherOutputStream.close()` cascades into closing the wrapped `FileOutputStream`.** The new streaming store tried to `fsync` the file *after* that close, on an already-closed descriptor — surfaced as `SyncFailedException` on the very first attachment test. Fixed by driving `Cipher.update()`/`doFinal()` manually instead of through `CipherOutputStream`, so the code controls exactly when the file closes. (The C# side never had this problem — `CryptoStream`'s constructor takes an explicit `leaveOpen: true`.)
2. **An empty-but-present attachment (`new byte[0]`) was silently treated as "no attachment at all".** `QueueFile(byte[])`'s wrapper only built a source stream `if(data.Length>0)`, collapsing a deliberate 0-byte file to `null` — which then failed message-content validation ("Messages must contain 1–2000 characters") for the existing, previously-passing "empty attachment" test. Fixed by always wrapping `data`, even when empty; `null` now means "no attachment" only when a caller (`Queue`) explicitly passes it.
3. **The most serious one: re-syncing a message the recipient already has could delete the attachment they already legitimately stored for it.** Both `Receive()`/`receive()` and the group SYNCREQ receive loop wrote the incoming stream to disk *before* checking whether the message was a duplicate, then deleted the file on any non-insert path — which didn't distinguish "genuinely rejected" from "a normal duplicate re-sync of something already present". Every group member re-syncs constantly with every other member sharing a group, so this reliably destroyed real attachments during ordinary background traffic, not just on some rare retry. Fixed by explicitly separating "cleared/hidden, never resurrect" (still deletes, correct — this is the existing "duplicate retries do not restore cleared history" guarantee) from "duplicate of a still-present message" (never deletes now) in both platforms' receive paths.
4. **Java sockets have no native write-timeout.** Unlike the read timeout set once at connect time (`setSoTimeout`), a stuck peer on the *reading* end could block a *sending* thread on `OutputStream.write()` forever — and with only 2–6 threads shared by a whole process for every peer's send and receive work, just one or two such stalls silently starved everything else. This surfaced as a real, reproducible test hang (a Seen receipt that should arrive in ~2 seconds just never did, no exception, no timeout, for 90+ real seconds) — traced by adding temporary file-based diagnostic logging directly into the delivery loop's catch blocks (since a background-thread exception doesn't reach the test harness's stdout protocol) until the exact stuck call was identified. Fixed with a watchdog (`NetworkTimeoutOutputStream`) that closes the socket if a single chunk write doesn't finish within 45 seconds — applied everywhere a raw socket write happens (the control-frame `write()` helper, avatar push, attachment send), not just the new streaming path.
5. **The actual root cause of that same hang, once found: `File.renameTo()` on Java silently returns `false` (doesn't throw) if the destination already exists — this is Windows-specific Java behavior, unlike a POSIX rename.** The new streaming store used this for its atomic temp-file-to-final-path swap; for an attachment already on disk (exactly the "duplicate re-sync" scenario above, after fixing bug 3) every retry failed identically and forever with "Cannot save attachment", which meant `deliver()` returned early on every cycle before ever reaching the unrelated Seen-receipt code further down the same method — explaining why an apparently unrelated feature (Seen) seemed to break. Root-caused by adding message-ID-level diagnostic logging and reading the exact exception, not by guessing. Fixed by switching to `java.nio.file.Files.move(..., REPLACE_EXISTING)`, which correctly replaces the destination on every platform — also applied to `setAvatar`/`setPeerAvatar`'s same `renameTo` pattern, a **pre-existing latent bug** from the 0.7.0/0.7.3 avatar work that just never happened to hit the "already exists" case in earlier tests (changing your avatar a *second* time would have silently failed the same way).
6. **Windows' picked-filename display name vs. on-disk name.** The old `Send()` always passed the explicit `pendingAttachmentName` through to the queue call; the new path-based `QueueFileFromPathAsync` re-derived the name from the file path instead, silently dropping a caller-supplied display name (harmless for a real file picker, where they're the same string, but a real behavior change). Caught by the native Windows UI test, which deliberately uses a display name that differs from the underlying temp file's name. Fixed by adding an optional `displayName` parameter that takes priority over the path-derived name.

Design summary (also in PROTOCOL.md): every attachment read/write, on both the local-storage and network path, now moves through fixed 1 MiB chunks instead of one large buffer — this is what makes 1 GiB (`MaxFileSize`/`MAX_FILE_SIZE`, up from 200 MiB) safe without an OOM risk, particularly on Android. New on-disk attachment format (`LMATCS1` magic + length-prefixed DPAPI/Keystore-wrapped per-attachment AES-256-CBC key/IV + streamed ciphertext) replaces whole-file `Protect`/`protect`; attachments from before 0.7.4 are still read correctly via the old path, auto-detected by the absence of the magic prefix — nothing is migrated or re-encrypted on upgrade. Wire bytes are unchanged (same frames, same total length — only the number of `read`/`write` calls moving them differs), so this is fully wire-compatible with older versions; an older peer is just still bound by its own lower cap. `Send()` on both platforms is now properly asynchronous/backgrounded (the actual fix for the original UI-freeze complaint), with a throttled `TransferProgress` event surfaced as "Sending NN%" directly in the message's own status line. Windows' attach flow now tracks a file *path* instead of reading the whole file into memory up front; Android's tracks a `Uri` (+ size, queried via `OpenableColumns`, with a bounded-read fallback for the rare picker that doesn't report size). Inline thumbnails (both draft preview and the chat feed itself) are now skipped above a 20 MB threshold, since decoding one means fully decrypting the attachment — otherwise scrolling a conversation with even one large file in it would silently decrypt it on every render.

Verification: added a new `tests/features.py` scenario sending a ~3 MB payload (several times the 1 MiB chunk size, so it actually exercises the chunking loop, not just a single-chunk payload that could hide a chunking bug) and verifying it round-trips byte-for-byte. Full suite (`tests/run.ps1`) passes at the *original* 30-second `wait_for` timeout (briefly raised to 90s mid-investigation specifically to rule out "just needs more time" before concluding bug 4/5 were genuine deadlocks, then put back). Windows UI re-confirmed via fresh `windows.png`/`windows-seen.png` screenshots, including the attachment-draft/inline-image/Save-Open-button assertions. `windows-dist-v074/LanMessenger.exe` and `LanMessenger-0.7.4.apk` (versionCode 13, existing signing key, `apksigner verify` OK) built standalone. **Not done, flagged explicitly rather than silently skipped:** an actual end-to-end ~1 GiB transfer has not been run on a real device/real Wi-Fi — the automated suite deliberately uses a much smaller multi-chunk payload to keep the suite fast; a real near-cap transfer is real acceptance-testing work, same category as every other physical-device gap already tracked in this file.

### 0.7.5 (both platforms) — conversation list: dynamic order + real photos, larger streaming chunks

User follow-up after the 1 GiB streaming work, four specific asks in one message, the last of which was a direct question to answer *before* making any change: (1) had the conversation list on Android not gotten the avatar treatment already (it hadn't — deliberately deferred back in 0.7.3, "can be extended later if asked"); (2) every device's conversation list ("main menu") must show a contact's real saved profile picture, not just a colored initial; (3) the list must be dynamic — whoever last sent you a message should sort to the top, not the existing name/online-status order; (4) the 1 MiB streaming chunk from 0.7.4 is too small, raise it to at least 3 MiB. Answered the validation question directly first, as asked: there is **no per-chunk integrity check** — one SHA-256 hash covers the whole reassembled attachment, verified only once the full transfer completes, exactly as scoped and agreed in 0.7.4; chunk size is unrelated to that guarantee either way.

**Dynamic ordering:** both platforms previously sorted the list by online-status-then-name (Windows) or online-then-name (Android) — never by activity. Now both compute each conversation's most recent message timestamp (`Messages(id).Max(m => m.Time)` / equivalent) and sort descending, mixing groups and 1:1 chats into one list exactly like WhatsApp — a fixed name/online order never reflected which conversation you'd actually want to see first. Windows: `ContactItem` gained a `LastActivity` field, computed once per `Render()` call; Android's `showPeople()`/`render()` was restructured from two separate loops (groups always first, then peers, each independently name-sorted) into one combined, sorted list — a real structural change, not just adding a comparator, since Android never mixed the two categories together before. Both platforms' re-render "signature" (the string compared to decide whether to actually rebuild the list, avoiding needless UI churn every second) had to include the computed last-activity value too, not just name/unread/online-status as before — otherwise sending your own message (which doesn't change your own unread count) wouldn't have moved that conversation to the top, since nothing in the old signature would have changed.

**Real photos in the list:** message-bubble avatars have shown a contact's real photo since 0.7.3; the conversation list itself still always drew a colored initial. Android: `addContactRow` gained a `View`-based overload (the old `String initial, int color` signature now just calls it with a `circle(...)` view), and a new `peerAvatarView()` helper decodes `e.peerAvatar(p.id)` when present, falling back to the initial circle otherwise — used only for 1:1 entries, not groups (a group has no single photo). Windows: this is an owner-drawn `ListBox` (`DrawItem` uses raw GDI+, not real controls per row), so decoding an avatar on every single paint would be wasteful — instead added an `avatarCache` dictionary populated once whenever the contact list actually rebuilds (inside the existing `signature != lastContacts` gate, which now also folds in each peer's `ReceivedAvatarHash` so a *new* photo triggers a cache refresh even when nothing else about that peer changed), and `DrawItem` just reads from that cache, clipping the drawn image to a circle via a `GraphicsPath` (the same clipping approach `RoundCorners` already uses elsewhere in this file, applied here to a paint call instead of a whole control).

**Chunk size:** `ChunkSize`/`CHUNK_SIZE` raised from 1 MiB to 3 MiB on both platforms — a one-line constant change; no other code needed adjusting since every chunked loop already worked in terms of "however many bytes fit in a chunk," not a hardcoded 1 MiB assumption anywhere.

**Also fixed in passing:** Android's About screen (`APP_VERSION` in `MainActivity.java`) has been a *separate* hardcoded constant from `AndroidManifest.xml`'s `versionName` this whole time, and had been silently stuck at `"0.7.2"` since the 0.7.3 round — three versions of drift nobody had a reason to notice until this pass touched nearby code. Bumped to match; **this is a standing gotcha worth remembering** — Android's version lives in two separate places (`AndroidManifest.xml` and this constant) and both must be bumped by hand every release, same caution already noted elsewhere in this file for `android/build.ps1`'s hardcoded output filename.

Verification: full suite passes; Windows UI re-confirmed via a fresh `windows-seen.png` that visibly shows the just-active "Design team" conversation sorted above the untouched "Project room" and "Phone test" entries — the ordering fix is directly visible in the screenshot, not just inferred from passing assertions. No avatar was set in that specific scripted UI scenario, so the list correctly still shows colored-initial circles there (same caveat as every prior avatar-related Windows screenshot — the photo-rendering path itself is exercised by the engine-level avatar-sharing tests, not by this particular native-UI screenshot). `windows-dist-v075/LanMessenger.exe` and `LanMessenger-0.7.5.apk` (versionCode 14, existing signing key, `apksigner verify` OK) built standalone.
